import 'package:flutter/material.dart';

class AppointmentOrganizerPage extends StatelessWidget {
  const AppointmentOrganizerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Text('Appointment Organizer Page'),
          ],
        ),
      ),
    );
  }
}
