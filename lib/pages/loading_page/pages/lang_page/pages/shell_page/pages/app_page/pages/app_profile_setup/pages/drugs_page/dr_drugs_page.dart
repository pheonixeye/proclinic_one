import 'package:flutter/material.dart';

class DrDrugsPage extends StatelessWidget {
  const DrDrugsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('dr_drugs_page'),
      ),
    );
  }
}
